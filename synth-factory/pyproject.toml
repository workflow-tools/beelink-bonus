[project]
name = "synth-factory"
version = "0.1.0"
description = "Config-driven synthetic data generation factory for the Beelink GTR9 Pro"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
